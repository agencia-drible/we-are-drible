{% extends 'layout' %}
{% block main %}
<main class="bd-masthead" id="content" role="main">
      <div class="container">

        {% for entry in craft.entries.section('webinicio') %}
        {% for block in entry.adicionar %}
        <div class="text-center">
          {{ block.conteudo }}
          <hr>
        </div>
        {% endfor %}
        {% endfor %}

      </div>
</main>
{% endblock %}
